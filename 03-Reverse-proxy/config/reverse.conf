server {
  listen 80;
  listen [::]:80;

  access_log /var/log/nginx/reverse-access.log;
  error_log /var/log/nginx/reverse-error.log;

  location /static {
    proxy_pass http://API-static-web;
  }
  location /dynamic {
    proxy_pass http://API-dynamic-web;
  }
}
